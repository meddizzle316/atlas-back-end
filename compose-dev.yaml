services:
  app:
    entrypoint:
    - sleep
    - infinity
    image: ubuntu
      build: Dockerfile
      context: ./.devcontainer
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock

